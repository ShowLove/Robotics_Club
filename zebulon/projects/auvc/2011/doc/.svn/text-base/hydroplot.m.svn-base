load "hydrosimp.log"
n=sqrt(size(hydrosimp,1));

x=reshape(hydrosimp(:,5),n,n);
y=reshape(hydrosimp(:,6),n,n);

subplot(2,2,1);
o=reshape(hydrosimp(:,1),n,n);
mesh(x,y,o);

subplot(2,2,2);
o=reshape(hydrosimp(:,2),n,n);
mesh(x,y,o);

subplot(2,2,3);
o=reshape(hydrosimp(:,3),n,n);
mesh(x,y,o);

subplot(2,2,4);
o=reshape(hydrosimp(:,4),n,n);
mesh(x,y,o);

pause