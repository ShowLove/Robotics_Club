//#include "ground/vision/objectdetection.h"
//using namespace Zebulon;
//using namespace Vision;
//ObjectDetection::ObjectDetection()
//{

//}

//ObjectDetection::~ObjectDetection()
//{

//}

//void ObjectDetection::AddObject(const VisionObject options, const std::string& name)
//{

//}

//IplImage * ObjectDetection::GetMask(IplImage* imgIn, const std::string& name)
//{
////    IplImage * mask = cvCreateImage(cvGetSize(imgIn),IPL_DEPTH_8U, 1);
////    IplImage * result = NULL;
////    Vision::VisionUtils::ExtractColor(imgIn,mask,mObstacleColor[0],100);
////    std::list<CvBox2D> blobs = Vision::VisionUtils::GetBlobBoxes(mask,0.01,1);
////    for(std::list<CvBox2D>::iterator i = blobs.begin(); i != blobs.end(); i++)
////    {
////        CvBox2D rotatedBox = *i;
////        rotatedBox.angle += 90;
////        Vision::VisionUtils::DrawSquare(mask,rotatedBox);
////    }
////    blobs = Vision::VisionUtils::GetBlobBoxes(mask,0.01,1);
////    for(std::list<CvBox2D>::iterator i = blobs.begin(); i != blobs.end(); i++)
////    {
////        CvPoint2D32f points[4];
////        cvBoxPoints(*i,points);
////        CvPoint point2d[4];
////        for(int i = 0; i < 4; i++)
////        {
////            point2d[i] = cvPointFrom32f(points[i]);
////        }
////        cvFillConvexPoly(mask,point2d,4,cvScalar(255,255,255));
////        //Vision::VisionUtils::DrawSquare(imgOut,*i);
////    }
////    cvDrawRect(mask,cvPoint(0,0),cvPoint(mask->width,mask->height),cvScalar(255,255,255),20);
////    cvAbsDiffS(mask,mask,cvScalar(255,255,255));
//}
