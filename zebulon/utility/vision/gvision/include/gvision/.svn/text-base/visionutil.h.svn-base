#include<opencv/cv.h>
#include<stdio.h>
#include<string.h>

#ifndef _VISION_UTIL
#define _VISION_UTIL

namespace Zebulon
{
    namespace Vision
    {
        class VisionUtil
        {
            public:
                static bool ResetImage(IplImage *Image);
                static bool CombineImages(IplImage *InputImage, IplImage *OutputImage);

        };
    }
}
#endif